---

apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy

metadata:
  name: kai-home-migrator

spec:
  imageRepositoryRef:
    name: kai-home-migrator
  policy:
    semver:
      range: 0.x-x

---

apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy

metadata:
  name: kai-justchat-web

spec:
  imageRepositoryRef:
    name: kai-justchat-web
  policy:
    semver:
      range: 0.x-x

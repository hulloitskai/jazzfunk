---

apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization

metadata:
  name: apps
  namespace: flux-system

spec:
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: systems
  path: cluster/apps
  prune: true
  validation: client
  interval: 2m0s

---

apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImageUpdateAutomation

metadata:
  name: flux-system
  namespace: flux-system

spec:
  git:
    checkout:
      ref:
        branch: main
    push:
      branch: main
    commit:
      author:
        name: fluxcdbot
        email: fluxcdbot@users.noreply.github.com
      messageTemplate: 🚢 Automated image updates
  interval: 2m0s
  sourceRef:
    kind: GitRepository
    name: flux-system
  update:
    path: apps
    strategy: Setters

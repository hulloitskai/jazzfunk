---

apiVersion: cert-manager.io/v1
kind: Certificate

metadata:
  name: bedda-travel

spec:
  secretName: bedda-travel-tls
  dnsNames:
    - bedda.travel
    - www.bedda.travel
    - images.bedda.travel
    - api.book.bedda.travel
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer

---

apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy

metadata:
  name: bedda-travel-pilot

spec:
  imageRepositoryRef:
    name: bedda-travel-pilot
  policy:
    semver:
      range: 0.x-x

---

apiVersion: v1
kind: Service

metadata:
  name: home-web
  labels:
    app: home
    component: web

spec:
  selector:
    app: home
    component: web
  ports:
    - name: http
      port: 80
      targetPort: http

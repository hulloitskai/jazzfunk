---

apiVersion: v1
kind: Service

metadata:
  name: justchat-web
  labels:
    app: justchat
    component: web

spec:
  selector:
    app: justchat
    component: web
  ports:
    - name: http
      port: 80
      targetPort: http

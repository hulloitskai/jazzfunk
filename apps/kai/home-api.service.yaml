---

apiVersion: v1
kind: Service

metadata:
  name: home-api
  labels:
    app: home
    component: api

spec:
  selector:
    app: home
    component: api
  ports:
    - name: http
      port: 80
      targetPort: http
